# Copy to .env.local for development. Never commit .env.local.

# Required in production. The deployment denies every request when this is unset - see
# lib/auth/access.ts. Generate one with: openssl rand -hex 32
APP_ACCESS_SECRET=

# Added in Phase 1.
# ANTHROPIC_API_KEY=
# DEEPGRAM_API_KEY=
